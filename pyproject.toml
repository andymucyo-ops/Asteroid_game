[project]
name = "asteroids"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.6"
dependencies = [
    "pygame==2.6.1",
]

[dependency-groups]
dev = [
    "ruff>=0.0.17",
]
